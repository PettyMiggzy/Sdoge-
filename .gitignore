.DS_Store
node_modules
*.log

# Hardhat (contracts/)
artifacts/
cache/
typechain-types/

# Generated NFT art/metadata (nft/) - not committed, collection not started yet
nft/assets/

# Real secrets for running bot/ yourself (pm2) - see bot/.env.example
.env

# tgpad runtime state: Telegram user ids, bans, launch records (no keys, but not public)
tgpad/data/

# Local deploy rehearsals (the real record is deployments/arc.json)
contracts/deployments/hardhat*
contracts/deployments/localhost*
